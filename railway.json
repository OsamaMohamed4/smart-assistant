{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "rm -rf public/admin && npm run build"
  },
  "deploy": {
    "startCommand": "node server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}